# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
.venv/
.uv/
*.egg-info/
.pytest_cache/
.ruff_cache/
.mypy_cache/

# Data — large, regenerable from sources
data/raw/papers/
data/raw/seed/
data/vectors/
data.duckdb
data.duckdb.wal

# Note: generated wiki content (docs/paper, docs/viz, etc.) IS committed
# so GitHub Pages can deploy it without running the LLM pipeline in CI.
# Re-run `llm-wiki all` locally to refresh.

# MkDocs
site/

# OS
.DS_Store

# Secrets
.env
.env.local

# Local pipeline backups
*.parquet.bak
